# Phase 1

Phase 1 establishes the Zodiacs SDK workspace, typed token registry, market
adapter contracts, React hook scaffolds, and minimal premium UI components.

The registry is local source-controlled data. Applications do not scrape
Zodiacs.org at runtime.
